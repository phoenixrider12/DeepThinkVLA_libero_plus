"""Single reasoning-steering function used by LIBERO evals.

Edit `steer_reasoning` to replace the VLA-generated reasoning trace at test
time. The eval script passes the current image, task prompt, and VLA reasoning,
and expects this function to return only the new reasoning string.

When `--use_reasoning_steering` is enabled, actions are decoded from the
reasoning string returned by this function.
"""

from __future__ import annotations

from typing import Optional

import numpy as np


def steer_reasoning(
    image: np.ndarray,
    prompt: str,
    current_reasoning: Optional[str],
) -> str:
    """Return the revised reasoning for the current observation and prompt.

    Args:
        image: The policy input image from the current observation.
        prompt: The task description passed to the VLA.
        current_reasoning: The reasoning text generated by the VLA.

    Returns:
        A new reasoning string.
    """
    return "asdf"
    # return current_reasoning or ""
